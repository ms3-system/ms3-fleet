apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: data-service
  labels:
    app.kubernetes.io/name: data-service
    app.kubernetes.io/part-of: ms3
spec:
  serviceName: "data-service"
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: data-service
      app.kubernetes.io/part-of: ms3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: data-service
        app.kubernetes.io/part-of: ms3
    spec:
      containers:
        - name: data-service
          image: ghcr.io/ms3-system/ms3/data-service:v0.1.0
          ports:
            - containerPort: 8081

          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi

          envFrom:
            - configMapRef:
                name: data-service-config
          volumeMounts:
            - name: data-service-vol
              mountPath: /app/data

          startupProbe:
            httpGet:
              path: /healthz/startup
              port: 8081
            failureThreshold: 30
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /healthz/live
              port: 8081
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /healthz/ready
              port: 8081
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: data-service-vol
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
